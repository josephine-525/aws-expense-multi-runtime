# Backend

AWS Lambda handler: `lambda/handler.py` (Python 3.12, API Gateway HTTP API).

Packaged by **expenseinfra** `terraform/` via `archive_file` → same path requirements as `../frontend/README.md`.
