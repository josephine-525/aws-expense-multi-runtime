# IRSA: annotation must match Terraform output eks_expense_backend_irsa_role_arn (GitLab EKS_EXPENSE_BACKEND_ROLE_ARN).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: expense-backend
  annotations:
    eks.amazonaws.com/role-arn: ${EKS_EXPENSE_BACKEND_ROLE_ARN}
